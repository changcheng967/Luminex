# NNUE Architecture Specification: Luminex V13

## 1. General Metadata
- **Network Name**: Luminex V13
- **Target Engine / Application**: Luminex (v6.2+)
- **Primary Use Case**: Chess Position Evaluation
- **Weight Quantization Scheme**: Post-hoc quantization (int16 feature transformer, int8 tail; per-channel scales; int64-internal FM block with export clamp — see §3)
- **Internal Output Scale**: int32 accumulator units → centipawns

---

## 2. Input Features & Transformer Layer
- **Feature Set Name**: HalfKAv2_hg-f (king-bucketed, factorized)
- **Input Feature Dimension**: `[24576]` per perspective (Sparse boolean vector)
- **Feature Breakdown**:
  1. **Feature Group 1**: HalfKAv2_hg piece-square × own-king-square (64 king squares × 384 piece-square slots)
  2. **Feature Group 2**: King-bucket factorizer — 64 king squares collapse to horizontally-mirrored king buckets — bucket count is an ablation-gated knob (4 initial, 8/16 to test); factorized (virtual) features share transformer columns across buckets, multiplying coverage without multiplying weight rows
- **Perspective Symmetry**: Dual Perspective (`side-to-move` / `other-side`)

---

## 3. Perspective Accumulator Subnet
- **Input Dimension**: `[24576]` (Sparse boolean)
- **Output Dimension**: `[768]` per perspective
- **Layer Breakdown**:
  - `Sparse Linear`: Input `[24576]` -> Output `[768]` | Weights: `FP<i16>`, Bias: `FP<i16>` (saturating int16 adds; incremental update O(features flipped))
  - `Activation`: SCReLU `clamp(in, 0, QA)²`
  - **Interaction Block (V13)**: Factorization-machine latent sums per perspective
    - Per-feature latent vectors `v_i ∈ R^32` (`FP<i16>`, k = 1..32), running sums per component: `S_k = Σ_{i active} v_{i,k} ∈ Z^32`, `Q_k = Σ_{i active} v_{i,k}² ∈ Z^32` — no norm, no reduction over k; `I` must stay 32-wide to match the concat below
    - Squaring is the only quadratic-range operation in the network: `S_k²` worst case (30 · i16max)² overflows int32 by 3 orders of magnitude, and `Q_k` overflows with three max-magnitude terms (3 × 32767² > int32max; two fit). `S` and `Q` therefore accumulate in `int64`; `I_k = ½(S_k² − Q_k)` is computed in int64 and narrowed with a calibrated right-shift sized for 30 worst-case active features. `S_k²` safety exists ONLY on this int64 path — an export clamp cannot cover it (30·8448 squared is ~30× int32max). The clamp `|v_i,k| ≤ 8448` (√(int32max/30)) guarantees only the `Q_k` term against an unshifted int32 fallback
    - Forward-time interaction vector: `I ∈ R^32` — all pairwise feature interactions at O(1) per flipped feature; single active feature must yield exactly `I = 0`
    - Alternative candidate for this slot (gated head-to-head): forward-time elementwise products `A⊙B` of the two 384-wide accumulator halves

---

## 4. Subnet Selection & Bucketing Logic
- **Routing Factor**: Non-pawn piece count, kings excluded (range 0..30 by construction — promotions convert pawns without raising the total above 32 men)
- **Bucket Selection Logic** (clamped at both ends, rounding-independent):
  `pc = clamp(piece_count, 0, 30); Index = clamp(pc / 4, 0, 7)`
- **Number of Buckets**: `8` (Main Subnets `0` through `7`)
- **Combined Perspective State**: Concatenated `[768 + 768 + 32 + 32]` tensor (both accumulators + both interaction vectors) fed into selected Main Subnet
- **Soft Interpolation (experiment flag)**: blend adjacent buckets by fractional phase, `eval = (1-t)·head[b] + t·head[min(b+1, 7)]` — the top-bucket clamp forces pure `head[7]` at maximum material (the start position hits this on move 1), instead of hard switching

---

## 5. Main Subnet Architecture

| Layer Index | Layer Type | Input Shape | Output Shape | Activation / Operation | Fixed-Point Type |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **L1** | Dense Linear | `[1600]` | `[16]` | None | `FP<i32, QA·QB>` |
| **L2** | Activation | `[16]` | `[16]` | SCReLU `clamp(0, QA)²` | `FP<i16, QA>` |
| **L3** | Dense Linear | `[16]` | `[32]` | None | `FP<i32, QA·QB>` |
| **L4** | Activation | `[32]` | `[32]` | SCReLU `clamp(0, QA)²` | `FP<i16, QA>` |
| **L5** | Dot Product | `[32]` | `[1]` | Output Dot Product with Weights | `FP<i32, QA·QB>` |

---

## 6. Output Conversion & Post-Processing
- **DOSL Linear Head (parallel, O(1))**: per-feature scalar weights summed incrementally per perspective; qsearch stand-pat reads `lin[stm]` directly without running the Main Subnet
- **Output Head**: selected (or blended) bucket head of the Main Subnet
- **Final Rescaling Formula**:
  $$
  \text{Value} = \frac{\text{int32}(in) \times \text{Multiplier}}{\text{Denominator}}
  $$
- **Final Return Type**: Signed Integer (`int32`) / Centipawns

---

## 7. Dataflow Execution Diagram (Text Pipeline)

```text
[ Board State ]
│
├──► Side-To-Move Perspective ──► Sparse Linear [768] ──► SCReLU ──┐
│            │                                                     ├─► Concat [1600]
│            └──► FM Latent Sums S,Q ──► I_k = ½(S_k²−Q_k) [32] ──────┤       │
│                                                                  │       ▼
└──► Opponent Perspective ──► Sparse Linear [768] ──► SCReLU ─────┤   Bucket Routing
             │                                                     │   clamp(pc/4, 0, 7),  pc=clamp(piece_count,0,30)
             └──► FM Latent Sums S,Q ──► I_k = ½(S_k²−Q_k) [32] ──────┘       │
                                    ┌─────────────────────────────────────┴──────────────────┐
                                    ▼                                                        ▼
                              [ Main Subnet 0 ]                                       [ Main Subnet 7 ]
                                    │                                                        │
                              Dense Linear (16)                                       Dense Linear (16)
                                    │                                                        │
                                  SCReLU                                                    SCReLU
                                    │                                                        │
                              Dense Linear (32)                                       Dense Linear (32)
                                    │                                                        │
                                  SCReLU                                                    SCReLU
                                    │                                                        │
                                Dot Product (1)                                         Dot Product (1)
                                    └────────────────────────────────────┬─────────────────┘
                                                                         │
                                    [ DOSL Linear Head lin[stm] ] ───► qsearch stand-pat (O(1) bypass)
                                                                         │
                                                                         ▼
                                                              Rescaling / int32
                                                                         ▼
                                                              [ Evaluation Output (cp) ]
```
